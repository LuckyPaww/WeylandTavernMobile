# Copy to keystore.properties (gitignored) and fill in your real values.
# Without this file present, release builds fall back to being unsigned —
# debug builds are unaffected either way.
storeFile=keystore/weylandtavern-release.keystore
storePassword=changeme
keyAlias=weylandtavern
keyPassword=changeme
